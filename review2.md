# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
HTMLのinputタグが自動で生成され、_methodという名前で、PUTという値を持った隠しデータが出力される

### findメソッドの引数に指定しているIDは何のIDか
todosテーブルのidカラムの中から、特定の1レコードを識別するためのID

### findメソッドで実行しているSQLは何か
SELECT * FROM todos WHERE id = ? LIMIT 1;

### findメソッドで取得できる値は何か
todosテーブルの中で、指定されたIDを持つ1行分の全てのデータを含むインスタンス

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
インスタンスが、すでにDBに存在するデータ（主キーを持つデータ）かどうかを基準に判断している

## Todo論理削除

### traitとclassの違いとは
trait　→　単体でインスタンス化することはできないが、1つのクラスに複数を組み込むことができる
class　→　単体でインスタンス化して使用できるが、継承できる親クラスは1つだけという制限がある

### traitを使用するメリットとは
クラスの継承制限に縛られず、複数のクラスで共通の機能を簡単に再利用できるため、コードの重複を防ぎ、
柔軟な設計が可能になること

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
TodoControllerのインスタンスが生成されたタイミング

### RequestクラスからFormRequestクラスに変更した理由
バリデーションのロジックをコントローラから切り離し、役割を分担させるため

### $errorsのhasメソッドの引数・返り値は何か
引数：バリデーションエラーを確認したいフィールド名(name属性の値)
返り値：指定したフィールドにエラーが存在するかどうかを示す真偽値

### $errorsのfirstメソッドの引数・返り値は何か
引数：エラーメッセージを取得したいフィールド名　(name属性の値)
返り値：指定したフィールドの最初のエラーメッセージ　(文字列)

### フレームワークとは何か
共通のルールに従って開発することでコードの構造が統一され、一定の品質を保ちながら、効率的に開発可能な
アプリケーション開発の土台や枠組みとなるソフトウェアのこと

### MVCはどういったアーキテクチャか
アプリケーションの機能を、Model（データ処理）・View（画面表示）・Controller（全体の制御）の3つの役割に
分割して開発するアーキテクチャ
それぞれの役割を分担させることで、再利用性や可読性が向上し、保守性を高めることができる

### ORMとは何か、またLaravelが使用しているORMは何か
クラスとDBのテーブルを関連付け、SQLを直接書かずに直感的な操作で、データの取得や保存を可能にする仕組みのこと
Laravelが使用しているORM：Eloquent(エロクアント)

### composer.json, composer.lockとは何か
composer.json
→プロジェクトで使用するライブラリの種類やバージョンを一括管理するための設計図
　特定のバージョンを固定するのではなく、^8.0（8.0以上9.0未満）のように導入したいバージョンの範囲を指定する
　役割を持つ

composer.lock
→プロジェクトに実際にインストールされたパッケージの正確なバージョンを記録したファイル
　チーム全員で同じバージョンを共有し、開発環境による動作のバラつきを防ぐために、自動生成・更新される

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリ内に保存されている
※プロジェクトを動かすための外部依存を管理する倉庫であり、原則として直接編集不可